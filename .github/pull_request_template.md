## The decision

<!-- One decision per PR — "warm the neutrals", not a whole colour pass. -->

## Tokens changed

| Token | Before | After | Why |
| --- | --- | --- | --- |
|  |  |  |  |

## Checks

- [ ] `tokens/tokens.json` and `tokens/tokens.css` were both re-exported from the workbench
- [ ] **Compare to baseline** run after the change — specimens reviewed for semantic clashes
- [ ] Contrast readings still pass on surface and dark surface
- [ ] Review status set (approved / rejected) on every touched token
